[user]
  name = Michael Tolly
  email = miketolly@gmail.com
[push]
  default = simple
[alias]
  au = add -u
  dc = diff --cached
  dcs = diff --cached --stat
  ci = commit
  ls = log --stat
  tree = log --graph --oneline --all
  s = status
